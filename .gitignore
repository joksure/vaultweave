# dependencies & build output
node_modules/
dist/
coverage/
*.tsbuildinfo

# secrets & local env — tokens must never be committed
.env
.env.*
!.env.example

# vaultweave runtime artefacts (state DB, cache, lock and reports)
vaultweave.db
vaultweave.db-*
.vaultweave.lock
.vaultweave-run-report.json
.vaultweave-extraction-cache.json

# Legacy names are kept ignored so older backup folders remain safe.

# legacy runtime artefacts
state.sqlite
*.sqlite-journal
# Legacy backup directory

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
